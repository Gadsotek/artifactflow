{
	auto_https off
}

http://app.artifactflow.test, http://localhost {
	@artifactRuntime remote_ip {$ARTIFACT_RUNTIME_CIDR}
	respond @artifactRuntime 403
	reverse_proxy {$APP_UPSTREAM}
}

http://artifacts.artifactflow.test {
	reverse_proxy {$ARTIFACT_UPSTREAM}
}
