# Local development compose file. It intentionally uses loopback-bound ports,
# local credentials, APP_DEBUG=true defaults, and non-TLS PostgreSQL. Do not use
# this file as a production deployment template.
services:
  db:
    image: postgres:17.2-alpine@sha256:7e5df973a74872482e320dcbdeb055e178d6f42de0558b083892c50cda833c96
    environment:
      POSTGRES_DB: ${DB_DATABASE:-artifactflow}
      POSTGRES_USER: ${DB_SUPERUSER:-postgres}
      POSTGRES_PASSWORD: ${DB_SUPERPASS:-postgres}
      APP_DB_USER: ${DB_USERNAME:-app_user}
      APP_DB_PASS: ${DB_PASSWORD:-app_local_password}
    ports:
      - "127.0.0.1:${DB_HOST_PORT:-55440}:5432"
    volumes:
      - db-data:/var/lib/postgresql/data
      - ./docker/db/init:/docker-entrypoint-initdb.d:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_SUPERUSER:-postgres} -d ${DB_DATABASE:-artifactflow}"]
      interval: 5s
      timeout: 5s
      retries: 12
      start_period: 10s
    networks:
      - default
      - artifact-runtime

  db-test:
    profiles: ["test"]
    image: postgres:17.2-alpine@sha256:7e5df973a74872482e320dcbdeb055e178d6f42de0558b083892c50cda833c96
    environment:
      POSTGRES_DB: ${TEST_DB_DATABASE:-artifactflow_test}
      POSTGRES_USER: ${TEST_DB_SUPERUSER:-postgres}
      POSTGRES_PASSWORD: ${TEST_DB_SUPERPASS:-postgres_test_password}
      APP_DB_USER: ${TEST_DB_USERNAME:-app_user}
      APP_DB_PASS: ${TEST_DB_PASSWORD:-app_local_password}
    ports:
      - "127.0.0.1:${TEST_DB_HOST_PORT:-55442}:5432"
    volumes:
      - db-test-data:/var/lib/postgresql/data
      - ./docker/db/init:/docker-entrypoint-initdb.d:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${TEST_DB_SUPERUSER:-postgres} -d ${TEST_DB_DATABASE:-artifactflow_test}"]
      interval: 5s
      timeout: 5s
      retries: 12
      start_period: 10s
    networks:
      - default
      - e2e-artifact-runtime

  image-parser:
    build:
      context: .
      dockerfile: Dockerfile
      target: image-parser
    image: artifactflow-image-parser:dev
    environment:
      IMAGE_PARSER_SHARED_SECRET: ${IMAGE_PARSER_SHARED_SECRET:-artifactflow-local-parser-secret-not-for-production}
      IMAGE_PARSER_MAX_CLOCK_SKEW_SECONDS: ${IMAGE_PARSER_MAX_CLOCK_SKEW_SECONDS:-120}
      IMAGE_PARSER_SOCKET_PATH: /run/artifactflow/image-parser/parser.sock
      PORT: 8080
    read_only: true
    tmpfs:
      - /tmp:rw,noexec,nosuid,size=16m
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    pids_limit: 32
    mem_limit: 512m
    cpus: 1.0
    network_mode: none
    volumes:
      - image-parser-socket:/run/artifactflow/image-parser
    depends_on:
      image-parser-socket-init:
        condition: service_completed_successfully
    healthcheck:
      test: ["CMD", "php", "/srv/image-parser/healthcheck.php"]
      interval: 5s
      timeout: 3s
      retries: 6
      start_period: 5s

  pdf-processor:
    build:
      context: ./pdf-processor-spike
      dockerfile: Dockerfile
      target: pdf-processor-service
    image: artifactflow-pdf-processor-service:local
    environment:
      PDF_PROCESSOR_SHARED_SECRET: ${PDF_PROCESSOR_SHARED_SECRET:-artifactflow-local-pdf-processor-secret-not-for-production}
      PDF_PROCESSOR_MAX_CLOCK_SKEW_SECONDS: 120
      PDF_PROCESSOR_SOCKET_PATH: /run/artifactflow/pdf-processor/processor.sock
      PORT: 8080
    read_only: true
    tmpfs:
      - /tmp:rw,noexec,nosuid,size=32m
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    pids_limit: 32
    mem_limit: 512m
    cpus: 1.0
    network_mode: none
    volumes:
      - pdf-processor-socket:/run/artifactflow/pdf-processor
    depends_on:
      pdf-processor-socket-init:
        condition: service_completed_successfully
    healthcheck:
      test: ["CMD", "php", "/srv/pdf-processor-spike/healthcheck.php"]
      interval: 1m
      start_interval: 5s
      timeout: 15s
      retries: 6
      start_period: 30s

  xlsx-processor:
    build:
      context: ./xlsx-processor-spike
      dockerfile: Dockerfile
      target: xlsx-processor-spike-service
    image: artifactflow-xlsx-processor-service:local
    environment:
      XLSX_PROCESSOR_SHARED_SECRET: ${XLSX_PROCESSOR_SHARED_SECRET:-artifactflow-local-xlsx-processor-secret-not-for-production}
      XLSX_PROCESSOR_SOCKET_PATH: /run/artifactflow/xlsx-processor/processor.sock
    read_only: true
    tmpfs:
      - /tmp:rw,noexec,nosuid,size=64m
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    pids_limit: 32
    mem_limit: 384m
    cpus: 1.0
    network_mode: none
    volumes:
      - xlsx-processor-socket:/run/artifactflow/xlsx-processor
    depends_on:
      xlsx-processor-socket-init:
        condition: service_completed_successfully
    healthcheck:
      test: ["CMD", "node", "/srv/xlsx-processor-spike/healthcheck.cjs"]
      interval: 30s
      timeout: 3s
      retries: 6
      start_period: 30s

  docx-processor:
    build:
      context: ./docx-processor
      dockerfile: Dockerfile
    image: artifactflow-docx-processor:local
    environment:
      DOCX_PROCESSOR_SHARED_SECRET: ${DOCX_PROCESSOR_SHARED_SECRET:-artifactflow-local-docx-processor-secret-not-for-production}
      DOCX_PROCESSOR_MAX_CLOCK_SKEW_SECONDS: 120
      DOCX_PROCESSOR_SOCKET_PATH: /run/artifactflow/docx-processor/processor.sock
    read_only: true
    tmpfs:
      - /tmp:rw,noexec,nosuid,nodev,size=192m,mode=1777
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    pids_limit: 128
    mem_limit: 768m
    cpus: 1.0
    network_mode: none
    ulimits:
      nofile:
        soft: 256
        hard: 256
    volumes:
      - docx-processor-socket:/run/artifactflow/docx-processor
    depends_on:
      docx-processor-socket-init:
        condition: service_completed_successfully
    healthcheck:
      test: ["CMD", "php", "/srv/docx-processor/healthcheck.php"]
      interval: 30s
      timeout: 8s
      retries: 6
      start_period: 15s

  app:
    build:
      context: .
      dockerfile: Dockerfile
      target: dev
      args:
        APP_UID: ${APP_UID:-1000}
        APP_GID: ${APP_GID:-1000}
        INSTALL_XDEBUG: ${INSTALL_XDEBUG:-0}
        INSTALL_PCOV: ${INSTALL_PCOV:-1}
    image: artifactflow-app:dev
    group_add:
      - "10002"
      - "10003"
      - "10004"
    environment: &app-environment
      APP_NAME: ${APP_NAME:-artifactflow}
      APP_ENV: ${APP_ENV:-local}
      APP_DEBUG: ${APP_DEBUG:-true}
      APP_URL: ${APP_URL:-http://localhost:18080}
      # The artifact origin must differ from the app origin by HOST, not just
      # port: cookies ignore the port, so localhost/127.0.0.1 keeps the app
      # session cookie off artifact requests.
      ARTIFACT_URL: ${ARTIFACT_URL:-http://127.0.0.1:18081}
      # Host-port mappings mirrored into the container so `artifactflow:doctor`
      # can warn when a port and its URL were not changed together.
      APP_HOST_PORT: ${APP_PORT:-18080}
      ARTIFACT_HOST_PORT: ${ARTIFACT_HOST_PORT:-18081}
      ARTIFACT_FRAME_ANCESTORS: ${ARTIFACT_FRAME_ANCESTORS:-http://localhost:18080}
      ARTIFACT_URL_SIGNING_KEY: ${ARTIFACT_URL_SIGNING_KEY:-}
      ARTIFACT_PREVIEW_URL_TTL_SECONDS: ${ARTIFACT_PREVIEW_URL_TTL_SECONDS:-60}
      DRAFT_PREVIEW_CAPABILITY_RATE_LIMIT_PER_MINUTE: ${DRAFT_PREVIEW_CAPABILITY_RATE_LIMIT_PER_MINUTE:-30}
      ARTIFACT_MAX_BYTES: ${ARTIFACT_MAX_BYTES:-10485760}
      ARTIFACT_DRAFT_PREVIEW_MAX_BODY: ${ARTIFACT_DRAFT_PREVIEW_MAX_BODY:-6MB}
      PAGE_IMAGE_MAX_BYTES: ${PAGE_IMAGE_MAX_BYTES:-5242880}
      PAGE_IMAGE_MAX_PIXELS: ${PAGE_IMAGE_MAX_PIXELS:-16777216}
      PAGE_IMAGE_MAX_DIMENSION: ${PAGE_IMAGE_MAX_DIMENSION:-16384}
      IMAGE_PARSER_ENABLED: ${IMAGE_PARSER_ENABLED:-true}
      IMAGE_PARSER_URL: ${IMAGE_PARSER_URL:-http://localhost}
      IMAGE_PARSER_SOCKET_PATH: /run/artifactflow/image-parser/parser.sock
      IMAGE_PARSER_SHARED_SECRET: ${IMAGE_PARSER_SHARED_SECRET:-artifactflow-local-parser-secret-not-for-production}
      IMAGE_PARSER_CONNECT_TIMEOUT_SECONDS: ${IMAGE_PARSER_CONNECT_TIMEOUT_SECONDS:-2}
      IMAGE_PARSER_TIMEOUT_SECONDS: ${IMAGE_PARSER_TIMEOUT_SECONDS:-12}
      PDF_PROCESSOR_ENABLED: ${PDF_PROCESSOR_ENABLED:-false}
      PDF_PROCESSOR_URL: ${PDF_PROCESSOR_URL:-http://localhost}
      PDF_PROCESSOR_SOCKET_PATH: /run/artifactflow/pdf-processor/processor.sock
      PDF_PROCESSOR_SHARED_SECRET: ${PDF_PROCESSOR_SHARED_SECRET:-artifactflow-local-pdf-processor-secret-not-for-production}
      PDF_PROCESSOR_CONNECT_TIMEOUT_SECONDS: ${PDF_PROCESSOR_CONNECT_TIMEOUT_SECONDS:-2}
      PDF_PROCESSOR_TIMEOUT_SECONDS: ${PDF_PROCESSOR_TIMEOUT_SECONDS:-15}
      XLSX_PROCESSOR_ENABLED: ${XLSX_PROCESSOR_ENABLED:-false}
      XLSX_PROCESSOR_URL: ${XLSX_PROCESSOR_URL:-http://localhost}
      XLSX_PROCESSOR_SOCKET_PATH: /run/artifactflow/xlsx-processor/processor.sock
      XLSX_PROCESSOR_SHARED_SECRET: ${XLSX_PROCESSOR_SHARED_SECRET:-artifactflow-local-xlsx-processor-secret-not-for-production}
      XLSX_PROCESSOR_CONNECT_TIMEOUT_SECONDS: ${XLSX_PROCESSOR_CONNECT_TIMEOUT_SECONDS:-2}
      XLSX_PROCESSOR_TIMEOUT_SECONDS: ${XLSX_PROCESSOR_TIMEOUT_SECONDS:-15}
      DOCX_PROCESSOR_ENABLED: ${DOCX_PROCESSOR_ENABLED:-false}
      DOCX_PROCESSOR_URL: ${DOCX_PROCESSOR_URL:-http://localhost}
      DOCX_PROCESSOR_SOCKET_PATH: /run/artifactflow/docx-processor/processor.sock
      DOCX_PROCESSOR_SHARED_SECRET: ${DOCX_PROCESSOR_SHARED_SECRET:-artifactflow-local-docx-processor-secret-not-for-production}
      DOCX_PROCESSOR_CONNECT_TIMEOUT_SECONDS: ${DOCX_PROCESSOR_CONNECT_TIMEOUT_SECONDS:-2}
      DOCX_PROCESSOR_TIMEOUT_SECONDS: ${DOCX_PROCESSOR_TIMEOUT_SECONDS:-35}
      IMAGE_NORMALIZATION_USER_PIXEL_BUDGET_PER_MINUTE: ${IMAGE_NORMALIZATION_USER_PIXEL_BUDGET_PER_MINUTE:-67108864}
      IMAGE_NORMALIZATION_INSTALLATION_PIXEL_BUDGET_PER_MINUTE: ${IMAGE_NORMALIZATION_INSTALLATION_PIXEL_BUDGET_PER_MINUTE:-268435456}
      IMAGE_NORMALIZATION_USER_WORK_BUDGET_PER_MINUTE: ${IMAGE_NORMALIZATION_USER_WORK_BUDGET_PER_MINUTE:-67108864}
      IMAGE_NORMALIZATION_INSTALLATION_WORK_BUDGET_PER_MINUTE: ${IMAGE_NORMALIZATION_INSTALLATION_WORK_BUDGET_PER_MINUTE:-268435456}
      APP_RUNTIME_ROLE: ${APP_RUNTIME_ROLE:-app}
      TRUSTED_PROXIES: ${TRUSTED_PROXIES:-127.0.0.1,::1,172.16.0.0/12}
      DEFAULT_THEME: ${DEFAULT_THEME:-system}
      LOG_CHANNEL: ${LOG_CHANNEL:-stack}
      LOG_LEVEL: ${LOG_LEVEL:-debug}
      DB_CONNECTION: pgsql
      DB_HOST: ${DB_HOST:-db}
      DB_PORT: ${DB_PORT:-5432}
      DB_DATABASE: ${DB_DATABASE:-artifactflow}
      DB_USERNAME: ${DB_USERNAME:-app_user}
      DB_PASSWORD: ${DB_PASSWORD:-app_local_password}
      DB_SSLMODE: ${DB_SSLMODE:-disable}
      DB_SSLROOTCERT: ${DB_SSLROOTCERT:-}
      BROADCAST_CONNECTION: ${BROADCAST_CONNECTION:-null}
      REVERB_APP_ID: ${REVERB_APP_ID:-artifactflow-local}
      REVERB_APP_KEY: ${REVERB_APP_KEY:-artifactflow-local-key}
      REVERB_APP_SECRET: ${REVERB_APP_SECRET:-}
      REVERB_HOST: ${REVERB_HOST:-localhost}
      REVERB_PORT: ${REVERB_PORT:-8080}
      REVERB_SCHEME: ${REVERB_SCHEME:-http}
      REVERB_PUBLIC_URL: ${REVERB_PUBLIC_URL:-http://localhost:8080}
      REVERB_ALLOWED_ORIGINS: ${REVERB_ALLOWED_ORIGINS:-http://localhost:18080}
      REVERB_APP_MAX_CONNECTIONS: ${REVERB_APP_MAX_CONNECTIONS:-1000}
      REVERB_APP_RATE_LIMITING_ENABLED: ${REVERB_APP_RATE_LIMITING_ENABLED:-true}
      VITE_REVERB_APP_KEY: ${VITE_REVERB_APP_KEY:-artifactflow-local-key}
      VITE_REVERB_HOST: ${VITE_REVERB_HOST:-localhost}
      VITE_REVERB_PORT: ${VITE_REVERB_PORT:-8080}
      VITE_REVERB_SCHEME: ${VITE_REVERB_SCHEME:-http}
      CACHE_STORE: ${CACHE_STORE:-file}
      SESSION_DRIVER: ${SESSION_DRIVER:-database}
      SESSION_DOMAIN: ${SESSION_DOMAIN:-}
      SESSION_SECURE_COOKIE: ${SESSION_SECURE_COOKIE:-false}
      SESSION_ENCRYPT: ${SESSION_ENCRYPT:-false}
      SESSION_HTTP_ONLY: ${SESSION_HTTP_ONLY:-true}
      SESSION_SAME_SITE: ${SESSION_SAME_SITE:-lax}
      QUEUE_CONNECTION: ${QUEUE_CONNECTION:-database}
      COMPOSER_ALLOW_SUPERUSER: "1"
      COMPOSER_HOME: /home/app/.composer
      XDEBUG_MODE: ${XDEBUG_MODE:-off}
      XDEBUG_CONFIG: "client_host=${XDEBUG_CLIENT_HOST:-host.docker.internal} client_port=${XDEBUG_CLIENT_PORT:-9010} idekey=${XDEBUG_IDEKEY:-VSCODE} start_with_request=${XDEBUG_START_WITH_REQUEST:-trigger}"
      MAIL_MAILER: ${MAIL_MAILER:-smtp}
      MAIL_HOST: ${MAIL_HOST:-mailpit}
      MAIL_PORT: ${MAIL_PORT:-1025}
      MAIL_USERNAME: ${MAIL_USERNAME:-}
      MAIL_PASSWORD: ${MAIL_PASSWORD:-}
      MAIL_ENCRYPTION: ${MAIL_ENCRYPTION:-null}
      MAIL_FROM_ADDRESS: ${MAIL_FROM_ADDRESS:-dev@artifactflow.local}
      MAIL_FROM_NAME: ${MAIL_FROM_NAME:-artifactflow}
      ARTIFACT_STORAGE_ROOT: ${ARTIFACT_STORAGE_ROOT:-/var/www/html/storage/app/private_artifacts}
    ports:
      - "127.0.0.1:${APP_PORT:-18080}:8000"
    volumes:
      - ./:/var/www/html:delegated
      - ./vendor:/var/www/html/vendor
      - node-modules:/var/www/html/node_modules
      - composer-cache:/home/app/.composer/cache
      - image-parser-socket:/run/artifactflow/image-parser:ro
      - pdf-processor-socket:/run/artifactflow/pdf-processor:ro
      - xlsx-processor-socket:/run/artifactflow/xlsx-processor:ro
      - docx-processor-socket:/run/artifactflow/docx-processor:ro
    extra_hosts:
      - "host.docker.internal:host-gateway"
    depends_on:
      db:
        condition: service_healthy
      image-parser:
        condition: service_healthy
      pdf-processor:
        condition: service_healthy
      xlsx-processor:
        condition: service_healthy
      docx-processor:
        condition: service_healthy
    networks:
      - default
    healthcheck:
      test: ["CMD-SHELL", "/var/www/html/docker/healthcheck-app.sh"]
      interval: 10s
      timeout: 5s
      retries: 12
      start_period: 20s

  artifact-host:
    image: artifactflow-app:dev
    build:
      context: .
      dockerfile: Dockerfile
      target: dev
      args:
        APP_UID: ${APP_UID:-1000}
        APP_GID: ${APP_GID:-1000}
        INSTALL_XDEBUG: ${INSTALL_XDEBUG:-0}
        INSTALL_PCOV: ${INSTALL_PCOV:-1}
    environment:
      <<: *app-environment
      APP_RUNTIME_ROLE: artifact-host
      APP_URL: ${ARTIFACT_URL:-http://127.0.0.1:18081}
      APP_HOST_PORT: ${ARTIFACT_HOST_PORT:-18081}
      # The app's Vite server writes public/hot into the shared checkout. XLSX
      # previews deliberately load only hashed same-origin build assets, so the
      # isolated artifact runtime must ignore that app-origin development marker.
      VITE_HOT_FILE: /tmp/artifactflow-artifact-host-no-hot
      SESSION_DRIVER: array
      QUEUE_CONNECTION: sync
      IMAGE_PARSER_URL: ""
      IMAGE_PARSER_SOCKET_PATH: ""
      IMAGE_PARSER_SHARED_SECRET: ""
      # The same installation gate controls PDF delivery, but this runtime still
      # receives no processor endpoint or credential.
      PDF_PROCESSOR_URL: ""
      PDF_PROCESSOR_SOCKET_PATH: ""
      PDF_PROCESSOR_SHARED_SECRET: ""
      # Delivery needs only the enablement gate; this runtime receives no XLSX
      # processor endpoint, socket, or shared credential.
      XLSX_PROCESSOR_URL: ""
      XLSX_PROCESSOR_SOCKET_PATH: ""
      XLSX_PROCESSOR_SHARED_SECRET: ""
      DOCX_PROCESSOR_URL: ""
      DOCX_PROCESSOR_SOCKET_PATH: ""
      DOCX_PROCESSOR_SHARED_SECRET: ""
    volumes: &app-volumes
      - ./:/var/www/html:delegated
      - ./vendor:/var/www/html/vendor
      - node-modules:/var/www/html/node_modules
      - composer-cache:/home/app/.composer/cache
    depends_on:
      db:
        condition: service_healthy
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    networks:
      - artifact-runtime
    healthcheck:
      test: ["CMD-SHELL", "/var/www/html/docker/healthcheck-app.sh"]
      interval: 10s
      timeout: 5s
      retries: 12
      start_period: 20s

  e2e-image-parser:
    profiles: ["e2e"]
    build:
      context: .
      dockerfile: Dockerfile
      target: image-parser
    image: artifactflow-image-parser:dev
    environment:
      IMAGE_PARSER_SHARED_SECRET: ${E2E_IMAGE_PARSER_SHARED_SECRET:-artifactflow-e2e-image-parser-secret}
      IMAGE_PARSER_MAX_CLOCK_SKEW_SECONDS: 120
      IMAGE_PARSER_SOCKET_PATH: /run/artifactflow/image-parser/parser.sock
      PORT: 8080
    read_only: true
    tmpfs:
      - /tmp:rw,noexec,nosuid,size=32m
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    pids_limit: 32
    mem_limit: 512m
    cpus: 1.0
    network_mode: none
    volumes:
      - e2e-image-parser-socket:/run/artifactflow/image-parser
    depends_on:
      e2e-image-parser-socket-init:
        condition: service_completed_successfully
    healthcheck:
      test: ["CMD", "php", "/srv/image-parser/healthcheck.php"]
      interval: 1m
      start_interval: 5s
      timeout: 3s
      retries: 6
      start_period: 5s

  e2e-pdf-processor:
    profiles: ["e2e"]
    build:
      context: ./pdf-processor-spike
      dockerfile: Dockerfile
      target: pdf-processor-service
    image: artifactflow-pdf-processor-service:local
    environment:
      PDF_PROCESSOR_SHARED_SECRET: ${E2E_PDF_PROCESSOR_SHARED_SECRET:-artifactflow-e2e-pdf-processor-secret}
      PDF_PROCESSOR_MAX_CLOCK_SKEW_SECONDS: 120
      PDF_PROCESSOR_SOCKET_PATH: /run/artifactflow/pdf-processor/processor.sock
      PORT: 8080
    read_only: true
    tmpfs:
      - /tmp:rw,noexec,nosuid,size=32m
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    pids_limit: 32
    mem_limit: 512m
    cpus: 1.0
    network_mode: none
    volumes:
      - e2e-pdf-processor-socket:/run/artifactflow/pdf-processor
    depends_on:
      e2e-pdf-processor-socket-init:
        condition: service_completed_successfully
    healthcheck:
      test: ["CMD", "php", "/srv/pdf-processor-spike/healthcheck.php"]
      interval: 1m
      start_interval: 5s
      timeout: 15s
      retries: 6
      start_period: 10s

  e2e-xlsx-processor:
    profiles: ["e2e"]
    build:
      context: ./xlsx-processor-spike
      dockerfile: Dockerfile
      target: xlsx-processor-spike-service
    image: artifactflow-xlsx-processor-service:local
    environment:
      XLSX_PROCESSOR_SHARED_SECRET: ${E2E_XLSX_PROCESSOR_SHARED_SECRET:-artifactflow-e2e-xlsx-processor-secret}
      XLSX_PROCESSOR_SOCKET_PATH: /run/artifactflow/xlsx-processor/processor.sock
    read_only: true
    tmpfs:
      - /tmp:rw,noexec,nosuid,size=64m
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    pids_limit: 32
    mem_limit: 384m
    cpus: 1.0
    network_mode: none
    volumes:
      - e2e-xlsx-processor-socket:/run/artifactflow/xlsx-processor
    depends_on:
      e2e-xlsx-processor-socket-init:
        condition: service_completed_successfully
    healthcheck:
      test: ["CMD", "node", "/srv/xlsx-processor-spike/healthcheck.cjs"]
      interval: 1m
      start_interval: 5s
      timeout: 3s
      retries: 6
      start_period: 30s

  e2e-docx-processor:
    profiles: ["e2e"]
    build:
      context: ./docx-processor
      dockerfile: Dockerfile
    image: artifactflow-docx-processor:local
    environment:
      DOCX_PROCESSOR_SHARED_SECRET: ${E2E_DOCX_PROCESSOR_SHARED_SECRET:-artifactflow-e2e-docx-processor-secret}
      DOCX_PROCESSOR_MAX_CLOCK_SKEW_SECONDS: 120
      DOCX_PROCESSOR_SOCKET_PATH: /run/artifactflow/docx-processor/processor.sock
    read_only: true
    tmpfs:
      - /tmp:rw,noexec,nosuid,nodev,size=192m,mode=1777
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    pids_limit: 128
    mem_limit: 768m
    cpus: 1.0
    network_mode: none
    ulimits:
      nofile:
        soft: 256
        hard: 256
    volumes:
      - e2e-docx-processor-socket:/run/artifactflow/docx-processor
    depends_on:
      e2e-docx-processor-socket-init:
        condition: service_completed_successfully
    healthcheck:
      test: ["CMD", "php", "/srv/docx-processor/healthcheck.php"]
      interval: 1m
      start_interval: 5s
      timeout: 8s
      retries: 6
      start_period: 10s

  e2e-app:
    profiles: ["e2e"]
    image: artifactflow-app:dev
    group_add:
      - "10002"
      - "10003"
      - "10004"
    build:
      context: .
      dockerfile: Dockerfile
      target: dev
      args:
        APP_UID: ${APP_UID:-1000}
        APP_GID: ${APP_GID:-1000}
        INSTALL_XDEBUG: ${INSTALL_XDEBUG:-0}
        INSTALL_PCOV: ${INSTALL_PCOV:-1}
    environment:
      <<: *app-environment
      APP_ENV: ${E2E_APP_ENV:-local}
      # e2e-only throwaway key (public; never reuse in a real deployment). It is a valid
      # 32-byte key, so it passes the strength check — it is safe ONLY because e2e runs
      # APP_ENV=local, where the production boot guard does not execute.
      APP_KEY: ${E2E_APP_KEY:-base64:YXJ0aWZhY3RmbG93LWUyZS1hcHAta2V5LTAwMDAwMDA=}
      APP_URL: ${E2E_APP_URL:-http://localhost:18180}
      ARTIFACT_URL: ${E2E_ARTIFACT_URL:-http://127.0.0.1:18181}
      APP_HOST_PORT: ${E2E_APP_PORT:-18180}
      ARTIFACT_HOST_PORT: ${E2E_ARTIFACT_HOST_PORT:-18181}
      ARTIFACT_FRAME_ANCESTORS: ${E2E_ARTIFACT_FRAME_ANCESTORS:-http://localhost:18180}
      # e2e-only throwaway signing key: rejected by the production boot guard; never reuse in a real deployment.
      ARTIFACT_URL_SIGNING_KEY: ${E2E_ARTIFACT_URL_SIGNING_KEY:-artifact-preview-test-signing-key}
      DB_HOST: db-test
      DB_DATABASE: ${E2E_DB_DATABASE:-artifactflow_test_e2e}
      DB_USERNAME: ${TEST_DB_USERNAME:-app_user}
      DB_PASSWORD: ${TEST_DB_PASSWORD:-app_local_password}
      CACHE_STORE: database
      QUEUE_CONNECTION: sync
      MAIL_MAILER: array
      TURNSTILE_SITE_KEY: ""
      TURNSTILE_SECRET_KEY: ""
      TURNSTILE_EXPECTED_HOSTNAME: ""
      E2E_TURNSTILE_APP_PORT: ${E2E_TURNSTILE_APP_PORT:-18182}
      ARTIFACT_STORAGE_ROOT: /var/www/html/storage/app/e2e_private_artifacts
      VITE_HOT_FILE: /tmp/artifactflow-e2e-no-hot
      # All browser projects share one loopback source IP and one limiter table.
      # Keep the suite from exhausting the cross-selector production ceiling;
      # focused feature tests verify that ceiling with deliberately low limits.
      EXTERNAL_SHARE_PUBLIC_IP_RATE_LIMIT_PER_MINUTE: ${E2E_EXTERNAL_SHARE_PUBLIC_IP_RATE_LIMIT_PER_MINUTE:-1000}
      IMAGE_PARSER_ENABLED: "true"
      IMAGE_PARSER_URL: http://localhost
      IMAGE_PARSER_SOCKET_PATH: /run/artifactflow/e2e-image-parser/parser.sock
      IMAGE_PARSER_SHARED_SECRET: ${E2E_IMAGE_PARSER_SHARED_SECRET:-artifactflow-e2e-image-parser-secret}
      PDF_PROCESSOR_ENABLED: "true"
      PDF_PROCESSOR_URL: http://localhost
      PDF_PROCESSOR_SOCKET_PATH: /run/artifactflow/e2e-pdf-processor/processor.sock
      PDF_PROCESSOR_SHARED_SECRET: ${E2E_PDF_PROCESSOR_SHARED_SECRET:-artifactflow-e2e-pdf-processor-secret}
      XLSX_PROCESSOR_ENABLED: "true"
      XLSX_PROCESSOR_URL: http://localhost
      XLSX_PROCESSOR_SOCKET_PATH: /run/artifactflow/e2e-xlsx-processor/processor.sock
      XLSX_PROCESSOR_SHARED_SECRET: ${E2E_XLSX_PROCESSOR_SHARED_SECRET:-artifactflow-e2e-xlsx-processor-secret}
      DOCX_PROCESSOR_ENABLED: "true"
      DOCX_PROCESSOR_URL: http://localhost
      DOCX_PROCESSOR_SOCKET_PATH: /run/artifactflow/e2e-docx-processor/processor.sock
      DOCX_PROCESSOR_SHARED_SECRET: ${E2E_DOCX_PROCESSOR_SHARED_SECRET:-artifactflow-e2e-docx-processor-secret}
    command: ["sh", "/var/www/html/docker/start-e2e-app.sh"]
    ports:
      - "127.0.0.1:${E2E_APP_PORT:-18180}:8000"
      - "127.0.0.1:${E2E_TURNSTILE_APP_PORT:-18182}:8001"
    volumes:
      - ./:/var/www/html:delegated
      - ./vendor:/var/www/html/vendor
      - node-modules:/var/www/html/node_modules
      - composer-cache:/home/app/.composer/cache
      - e2e-artifacts:/var/www/html/storage/app/e2e_private_artifacts
      - e2e-image-parser-socket:/run/artifactflow/e2e-image-parser:ro
      - e2e-pdf-processor-socket:/run/artifactflow/e2e-pdf-processor:ro
      - e2e-xlsx-processor-socket:/run/artifactflow/e2e-xlsx-processor:ro
      - e2e-docx-processor-socket:/run/artifactflow/e2e-docx-processor:ro
    extra_hosts:
      - "host.docker.internal:host-gateway"
    depends_on:
      db-test:
        condition: service_healthy
      e2e-image-parser:
        condition: service_healthy
      e2e-pdf-processor:
        condition: service_healthy
      e2e-xlsx-processor:
        condition: service_healthy
      e2e-docx-processor:
        condition: service_healthy
    networks:
      - default
    healthcheck:
      test: ["CMD-SHELL", "/var/www/html/docker/healthcheck-app.sh && PORT=8001 /var/www/html/docker/healthcheck-app.sh"]
      interval: 10s
      timeout: 5s
      retries: 12
      start_period: 20s

  e2e-artifact-host:
    profiles: ["e2e"]
    image: artifactflow-app:dev
    build:
      context: .
      dockerfile: Dockerfile
      target: dev
      args:
        APP_UID: ${APP_UID:-1000}
        APP_GID: ${APP_GID:-1000}
        INSTALL_XDEBUG: ${INSTALL_XDEBUG:-0}
        INSTALL_PCOV: ${INSTALL_PCOV:-1}
    environment:
      <<: *app-environment
      APP_ENV: ${E2E_APP_ENV:-local}
      # e2e-only throwaway key (public; never reuse in a real deployment). It is a valid
      # 32-byte key, so it passes the strength check — it is safe ONLY because e2e runs
      # APP_ENV=local, where the production boot guard does not execute.
      APP_KEY: ${E2E_APP_KEY:-base64:YXJ0aWZhY3RmbG93LWUyZS1hcHAta2V5LTAwMDAwMDA=}
      APP_RUNTIME_ROLE: artifact-host
      APP_URL: ${E2E_ARTIFACT_URL:-http://127.0.0.1:18181}
      ARTIFACT_URL: ${E2E_ARTIFACT_URL:-http://127.0.0.1:18181}
      APP_HOST_PORT: ${E2E_ARTIFACT_HOST_PORT:-18181}
      ARTIFACT_HOST_PORT: ${E2E_ARTIFACT_HOST_PORT:-18181}
      ARTIFACT_FRAME_ANCESTORS: ${E2E_ARTIFACT_FRAME_ANCESTORS:-http://localhost:18180}
      # e2e-only throwaway signing key: rejected by the production boot guard; never reuse in a real deployment.
      ARTIFACT_URL_SIGNING_KEY: ${E2E_ARTIFACT_URL_SIGNING_KEY:-artifact-preview-test-signing-key}
      DB_HOST: db-test
      DB_DATABASE: ${E2E_DB_DATABASE:-artifactflow_test_e2e}
      DB_USERNAME: ${TEST_DB_USERNAME:-app_user}
      DB_PASSWORD: ${TEST_DB_PASSWORD:-app_local_password}
      SESSION_DRIVER: array
      QUEUE_CONNECTION: sync
      MAIL_MAILER: array
      TURNSTILE_SITE_KEY: ""
      TURNSTILE_SECRET_KEY: ""
      TURNSTILE_EXPECTED_HOSTNAME: ""
      IMAGE_PARSER_URL: ""
      IMAGE_PARSER_SOCKET_PATH: ""
      IMAGE_PARSER_SHARED_SECRET: ""
      # PDF delivery is under test here, but the artifact runtime still receives
      # no processor endpoint or credential.
      PDF_PROCESSOR_ENABLED: "true"
      PDF_PROCESSOR_URL: ""
      PDF_PROCESSOR_SOCKET_PATH: ""
      PDF_PROCESSOR_SHARED_SECRET: ""
      XLSX_PROCESSOR_ENABLED: "true"
      XLSX_PROCESSOR_URL: ""
      XLSX_PROCESSOR_SOCKET_PATH: ""
      XLSX_PROCESSOR_SHARED_SECRET: ""
      DOCX_PROCESSOR_ENABLED: "true"
      DOCX_PROCESSOR_URL: ""
      DOCX_PROCESSOR_SOCKET_PATH: ""
      DOCX_PROCESSOR_SHARED_SECRET: ""
      ARTIFACT_STORAGE_ROOT: /var/www/html/storage/app/e2e_private_artifacts
      VITE_HOT_FILE: /tmp/artifactflow-e2e-no-hot
    volumes:
      - ./:/var/www/html:delegated
      - ./vendor:/var/www/html/vendor
      - node-modules:/var/www/html/node_modules
      - composer-cache:/home/app/.composer/cache
      - e2e-artifacts:/var/www/html/storage/app/e2e_private_artifacts
    depends_on:
      db-test:
        condition: service_healthy
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    networks:
      - e2e-artifact-runtime
    healthcheck:
      test: ["CMD-SHELL", "/var/www/html/docker/healthcheck-app.sh"]
      interval: 10s
      timeout: 5s
      retries: 12
      start_period: 20s

  e2e-edge:
    profiles: ["e2e"]
    image: caddy:2.9.1-alpine@sha256:b4e3952384eb9524a887633ce65c752dd7c71314d2c2acf98cd5c715aaa534f0
    environment:
      APP_UPSTREAM: e2e-app:8000
      ARTIFACT_UPSTREAM: e2e-artifact-host:8000
      ARTIFACT_RUNTIME_CIDR: ${E2E_ARTIFACT_RUNTIME_NETWORK_SUBNET:-10.254.72.0/24}
    volumes:
      - ./docker/Caddyfile.local:/etc/caddy/Caddyfile:ro
    depends_on:
      e2e-app:
        condition: service_healthy
      e2e-artifact-host:
        condition: service_healthy
    networks:
      - default
      - e2e-artifact-runtime

  image-parser-socket-init:
    image: artifactflow-image-parser:dev
    user: "0:0"
    entrypoint: ["/bin/sh"]
    command: ["-c", "chmod 0755 /socket && chown 10001:10001 /socket"]
    network_mode: none
    read_only: true
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    cap_add:
      - CHOWN
      - FOWNER
    restart: "no"
    volumes:
      - image-parser-socket:/socket

  pdf-processor-socket-init:
    image: artifactflow-pdf-processor-service:local
    user: "0:0"
    entrypoint: ["/bin/sh"]
    command: ["-c", "chmod 0755 /socket && chown 10002:10002 /socket"]
    network_mode: none
    read_only: true
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    cap_add:
      - CHOWN
      - FOWNER
    restart: "no"
    volumes:
      - pdf-processor-socket:/socket

  e2e-pdf-processor-socket-init:
    profiles: ["e2e"]
    image: artifactflow-pdf-processor-service:local
    user: "0:0"
    entrypoint: ["/bin/sh"]
    command: ["-c", "chmod 0755 /socket && chown 10002:10002 /socket"]
    network_mode: none
    read_only: true
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    cap_add:
      - CHOWN
      - FOWNER
    restart: "no"
    volumes:
      - e2e-pdf-processor-socket:/socket

  xlsx-processor-socket-init:
    image: artifactflow-xlsx-processor-service:local
    user: "0:0"
    entrypoint: ["/bin/sh"]
    command: ["-c", "chmod 0755 /socket && chown 10003:10003 /socket"]
    network_mode: none
    read_only: true
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    cap_add:
      - CHOWN
      - FOWNER
    restart: "no"
    volumes:
      - xlsx-processor-socket:/socket

  e2e-xlsx-processor-socket-init:
    profiles: ["e2e"]
    image: artifactflow-xlsx-processor-service:local
    user: "0:0"
    entrypoint: ["/bin/sh"]
    command: ["-c", "chmod 0755 /socket && chown 10003:10003 /socket"]
    network_mode: none
    read_only: true
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    cap_add:
      - CHOWN
      - FOWNER
    restart: "no"
    volumes:
      - e2e-xlsx-processor-socket:/socket

  docx-processor-socket-init:
    image: artifactflow-docx-processor:local
    user: "0:0"
    entrypoint: ["/bin/sh"]
    command: ["-c", "chmod 0755 /socket && chown 10004:10004 /socket"]
    network_mode: none
    read_only: true
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    cap_add:
      - CHOWN
      - FOWNER
    restart: "no"
    volumes:
      - docx-processor-socket:/socket

  e2e-docx-processor-socket-init:
    profiles: ["e2e"]
    image: artifactflow-docx-processor:local
    user: "0:0"
    entrypoint: ["/bin/sh"]
    command: ["-c", "chmod 0755 /socket && chown 10004:10004 /socket"]
    network_mode: none
    read_only: true
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    cap_add:
      - CHOWN
      - FOWNER
    restart: "no"
    volumes:
      - e2e-docx-processor-socket:/socket

  e2e-image-parser-socket-init:
    profiles: ["e2e"]
    image: artifactflow-image-parser:dev
    user: "0:0"
    entrypoint: ["/bin/sh"]
    command: ["-c", "chmod 0755 /socket && chown 10001:10001 /socket"]
    network_mode: none
    read_only: true
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    cap_add:
      - CHOWN
      - FOWNER
    restart: "no"
    volumes:
      - e2e-image-parser-socket:/socket

  artifact-gateway:
    image: caddy:2.9.1-alpine@sha256:b4e3952384eb9524a887633ce65c752dd7c71314d2c2acf98cd5c715aaa534f0
    environment:
      ARTIFACT_UPSTREAM: artifact-host:8000
    ports:
      - "127.0.0.1:${ARTIFACT_HOST_PORT:-18081}:80"
    volumes:
      - ./docker/Caddyfile.artifact-gateway:/etc/caddy/Caddyfile:ro
    depends_on:
      artifact-host:
        condition: service_healthy
    security_opt:
      - no-new-privileges:true
    networks:
      - default
      - artifact-runtime

  e2e-artifact-gateway:
    profiles: ["e2e"]
    image: caddy:2.9.1-alpine@sha256:b4e3952384eb9524a887633ce65c752dd7c71314d2c2acf98cd5c715aaa534f0
    environment:
      ARTIFACT_UPSTREAM: e2e-artifact-host:8000
    ports:
      - "127.0.0.1:${E2E_ARTIFACT_HOST_PORT:-18181}:80"
    volumes:
      - ./docker/Caddyfile.artifact-gateway:/etc/caddy/Caddyfile:ro
    depends_on:
      e2e-artifact-host:
        condition: service_healthy
    security_opt:
      - no-new-privileges:true
    networks:
      - default
      - e2e-artifact-runtime

  worker:
    image: artifactflow-app:dev
    command: ["sh", "/var/www/html/docker/start-worker.sh"]
    environment:
      <<: *app-environment
      APP_RUNTIME_ROLE: worker
      IMAGE_PARSER_URL: ""
      IMAGE_PARSER_SOCKET_PATH: ""
      IMAGE_PARSER_SHARED_SECRET: ""
      PDF_PROCESSOR_ENABLED: "false"
      PDF_PROCESSOR_URL: ""
      PDF_PROCESSOR_SOCKET_PATH: ""
      PDF_PROCESSOR_SHARED_SECRET: ""
      XLSX_PROCESSOR_ENABLED: "false"
      XLSX_PROCESSOR_URL: ""
      XLSX_PROCESSOR_SOCKET_PATH: ""
      XLSX_PROCESSOR_SHARED_SECRET: ""
      DOCX_PROCESSOR_ENABLED: "false"
      DOCX_PROCESSOR_URL: ""
      DOCX_PROCESSOR_SOCKET_PATH: ""
      DOCX_PROCESSOR_SHARED_SECRET: ""
    volumes: *app-volumes
    depends_on:
      app:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "/var/www/html/docker/healthcheck-app.sh"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 20s

  scheduler:
    image: artifactflow-app:dev
    command: ["sh", "/var/www/html/docker/start-scheduler.sh"]
    environment:
      <<: *app-environment
      APP_RUNTIME_ROLE: scheduler
      IMAGE_PARSER_URL: ""
      IMAGE_PARSER_SOCKET_PATH: ""
      IMAGE_PARSER_SHARED_SECRET: ""
      PDF_PROCESSOR_ENABLED: "false"
      PDF_PROCESSOR_URL: ""
      PDF_PROCESSOR_SOCKET_PATH: ""
      PDF_PROCESSOR_SHARED_SECRET: ""
      XLSX_PROCESSOR_ENABLED: "false"
      XLSX_PROCESSOR_URL: ""
      XLSX_PROCESSOR_SOCKET_PATH: ""
      XLSX_PROCESSOR_SHARED_SECRET: ""
      DOCX_PROCESSOR_ENABLED: "false"
      DOCX_PROCESSOR_URL: ""
      DOCX_PROCESSOR_SOCKET_PATH: ""
      DOCX_PROCESSOR_SHARED_SECRET: ""
    volumes: *app-volumes
    depends_on:
      app:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "/var/www/html/docker/healthcheck-app.sh"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 20s

  reverb:
    profiles: ["realtime"]
    image: artifactflow-app:dev
    command: ["sh", "-lc", "php artisan reverb:start --host=0.0.0.0 --port=8080"]
    environment:
      <<: *app-environment
      # APP_KEY is not in the shared anchor (the app service reads it from the mounted
      # .env, which artifactflow:install / key:generate populates). In normal operation
      # this worker reads .env the same way; the pass-through below only matters for the
      # verify-reverb-origin gate, which exports a production APP_KEY (and APP_ENV=production)
      # so the worker boots the real production security gate. That boot gate -- not this
      # line -- is the fail-closed check that rejects a missing or weak key, so keep this a
      # plain pass-through. Never a ":-" empty default: that form silently shadows the
      # mounted key (see E2eIsolationConfigurationTest) and Compose cannot hard-require the
      # key here because it is not present at compose-interpolation time for other commands.
      APP_KEY: ${APP_KEY}
      APP_RUNTIME_ROLE: worker
      # Reverb reads its restart signal through the default cache store. Normal local
      # operation therefore needs the same database credential as the app. The origin
      # probe selects an in-memory restart cache and supplies a fresh non-placeholder
      # sentinel only to satisfy the production configuration gate without connecting.
      DB_PASSWORD: ${REVERB_SMOKE_DB_PASSWORD:-${DB_PASSWORD:-app_local_password}}
      # This worker never accepts image uploads. Do not give an unrelated runtime
      # the parser endpoint or its HMAC credential merely because it shares the
      # application environment anchor.
      IMAGE_PARSER_URL: ""
      IMAGE_PARSER_SOCKET_PATH: ""
      IMAGE_PARSER_SHARED_SECRET: ""
      PDF_PROCESSOR_ENABLED: "false"
      PDF_PROCESSOR_URL: ""
      PDF_PROCESSOR_SOCKET_PATH: ""
      PDF_PROCESSOR_SHARED_SECRET: ""
      XLSX_PROCESSOR_ENABLED: "false"
      XLSX_PROCESSOR_URL: ""
      XLSX_PROCESSOR_SOCKET_PATH: ""
      XLSX_PROCESSOR_SHARED_SECRET: ""
      DOCX_PROCESSOR_ENABLED: "false"
      DOCX_PROCESSOR_URL: ""
      DOCX_PROCESSOR_SOCKET_PATH: ""
      DOCX_PROCESSOR_SHARED_SECRET: ""
      # Reverb must share the app's cache store so `reverb:restart` crosses
      # processes. Before first-install migrations exist, both fall back to the
      # shared file cache. The isolated origin probe overrides only this store;
      # limiter stores stay separate.
      CACHE_STORE: ${REVERB_CACHE_STORE:-${CACHE_STORE:-file}}
      CACHE_LIMITER: ${CACHE_LIMITER:-}
      ARTIFACT_CACHE_LIMITER: ${ARTIFACT_CACHE_LIMITER:-database_artifact_limiter}
      BROADCAST_CONNECTION: reverb
      REVERB_SERVER_HOST: 0.0.0.0
      REVERB_SERVER_PORT: 8080
    ports:
      - "127.0.0.1:${REVERB_PORT:-8080}:8080"
    volumes: *app-volumes
    depends_on:
      app:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "php -r '$$port=(int) (getenv(\"REVERB_SERVER_PORT\") ?: 8080); $$socket=@fsockopen(\"127.0.0.1\", $$port); if (is_resource($$socket)) { fclose($$socket); exit(0); } exit(1);'"]
      interval: 2s
      timeout: 2s
      retries: 30
      start_period: 5s

  vite:
    profiles: ["frontend"]
    # Must match the node digest of the Dockerfile frontend-build stage (Dependabot
    # only tracks the Dockerfile pin) so local dev and production assets build on
    # the same Node.
    image: node:26-alpine@sha256:aadf416b2cdce311a8811ba3f0608a61b77dbf997500e2eafe781b51f6a0b019
    working_dir: /var/www/html
    command: >
      sh -lc '
        set -eu;
        /var/www/html/docker/ensure-node-modules.sh;
        npm run dev -- --host 0.0.0.0 --port ${VITE_PORT:-5181}
      '
    environment:
      CHOKIDAR_USEPOLLING: ${CHOKIDAR_USEPOLLING:-0}
      VITE_PORT: ${VITE_PORT:-5181}
      WATCHPACK_POLLING: ${WATCHPACK_POLLING:-false}
    ports:
      - "127.0.0.1:${VITE_PORT:-5181}:5181"
    volumes:
      - ./:/var/www/html:delegated
      - node-modules:/var/www/html/node_modules
      - npm-cache:/home/node/.npm
    depends_on:
      app:
        condition: service_healthy

  edge:
    profiles: ["edge"]
    image: caddy:2.9.1-alpine@sha256:b4e3952384eb9524a887633ce65c752dd7c71314d2c2acf98cd5c715aaa534f0
    environment:
      APP_UPSTREAM: app:8000
      ARTIFACT_UPSTREAM: artifact-host:8000
      ARTIFACT_RUNTIME_CIDR: ${ARTIFACT_RUNTIME_NETWORK_SUBNET:-10.254.71.0/24}
    ports:
      - "127.0.0.1:${EDGE_HTTP_PORT:-18085}:80"
    volumes:
      - ./docker/Caddyfile.local:/etc/caddy/Caddyfile:ro
    depends_on:
      app:
        condition: service_healthy
      artifact-host:
        condition: service_healthy
    networks:
      - default
      - artifact-runtime

  # Local-only diagnostic profiles. Keep these loopback-bound and do not enable
  # them on shared, staging, or production hosts.
  adminer:
    profiles: ["adminer"]
    image: adminer:4.8.1-standalone@sha256:34d37131366c5aa84e1693dbed48593ed6f95fb450b576c1a7a59d3a9c9e8802
    environment:
      ADMINER_DEFAULT_SERVER: ${ADMINER_DEFAULT_SERVER:-db}
      ADMINER_DESIGN: ${ADMINER_DESIGN:-dracula}
    ports:
      - "127.0.0.1:${ADMINER_PORT:-18089}:8080"
    depends_on:
      db:
        condition: service_healthy

  mailpit:
    profiles: ["mail"]
    image: axllent/mailpit:v1.22@sha256:f7f7c31de4de59540ad6515a0ca057a77525bca2069b6e747d873ca66c10fe08
    environment:
      MP_SMTP_AUTH_ACCEPT_ANY: "1"
      MP_SMTP_AUTH_ALLOW_INSECURE: "1"
    ports:
      - "127.0.0.1:${MAILPIT_SMTP_PORT:-11033}:1025"
      - "127.0.0.1:${MAILPIT_WEB_PORT:-18033}:8025"

volumes:
  db-data:
  db-test-data:
  e2e-artifacts:
  image-parser-socket:
  e2e-image-parser-socket:
  pdf-processor-socket:
  e2e-pdf-processor-socket:
  xlsx-processor-socket:
  e2e-xlsx-processor-socket:
  docx-processor-socket:
  e2e-docx-processor-socket:
  node-modules:
  composer-cache:
  npm-cache:

networks:
  artifact-runtime:
    internal: true
    ipam:
      config:
        - subnet: ${ARTIFACT_RUNTIME_NETWORK_SUBNET:-10.254.71.0/24}
  e2e-artifact-runtime:
    internal: true
    ipam:
      config:
        - subnet: ${E2E_ARTIFACT_RUNTIME_NETWORK_SUBNET:-10.254.72.0/24}
